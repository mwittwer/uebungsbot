# Kopie als .streamlit/secrets.toml anlegen (lokal) bzw. den Inhalt bei
# Streamlit Community Cloud unter Settings -> Secrets einfuegen.
# Diese Datei niemals mit echtem Key committen - .gitignore schuetzt secrets.toml.
ANTHROPIC_API_KEY = "sk-ant-..."
